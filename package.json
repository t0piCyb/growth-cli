{
  "name": "growth-cli",
  "version": "0.5.0",
  "description": "CLI for the growth API",
  "type": "module",
  "bin": {
    "growth-cli": "./dist/growth-cli.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/growth-cli.js --target bun",
    "dev": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
